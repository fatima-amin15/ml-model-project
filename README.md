# ML Model Project

A machine learning project focused on screen time prediction using survey data.

## Setup

```bash
conda create -n ml-model python=3.10 -y
conda activate ml-model
pip install -r requirements.txt
